❯ python sim.py --query files/queries/example.json -h
usage: sim.py [-h] --query QUERY [--relations RELATIONS]

options:
  -h, --help            show this help message and exit
  --query QUERY         Path to query JSON file
  --relations RELATIONS
                        Path to relations JSON file
